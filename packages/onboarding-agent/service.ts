import {z} from 'zod';
import {DomainError} from '../domain';
import {owned,type Actor,type FormationRecord} from '../application';
import type {Repository,Row} from '../persistence';
import {prepareWyomingPacket} from '../formation-packet/wyoming';
import {prepareDelawarePacket} from '../formation-packet/delaware';
import {prepareEstoniaPacket} from '../formation-packet/estonia';
import {prepareUkPacket} from '../formation-packet/uk';
import {applyConfirmedPatch,assistantFor,deterministicExtract,fieldsForJurisdiction,initialConversationState,intakeSchemaFor,nextMissing,permittedFieldsForMessage,safeSyntheticMessage,type ConversationJurisdiction,type ProposedUpdate} from './index';
import {extractWithOpenAI,type OpenAIConfiguration} from './openai';

export interface ConversationRecord extends Row{id:string;organization_id:string;case_id:string;jurisdiction_code:ConversationJurisdiction;status:string;execution_mode:string;model:string|null;synthetic:boolean;state_json:unknown;pending_patch:unknown;revision:number;client_request_id:string;created_by:string;created_at:string;updated_at:string}
export interface ConversationTurn extends Row{id:string;organization_id:string;conversation_id:string;turn_kind:string;customer_message:string|null;assistant_message:string;proposed_patch:unknown;model_status:string;client_request_id:string;created_at:string}
const startSchema=z.object({caseId:z.uuid(),caseRevision:z.number().int().nonnegative(),synthetic:z.literal(true),clientRequestId:z.uuid()}).strict();
const messageSchema=z.object({conversationId:z.uuid(),revision:z.number().int().nonnegative(),message:z.string(),clientRequestId:z.uuid()}).strict();
const confirmationSchema=z.object({conversationId:z.uuid(),revision:z.number().int().nonnegative(),accept:z.boolean(),clientRequestId:z.uuid()}).strict();
const supportedJurisdiction=z.enum(['US-WY','US-DE','EE','GB']);
const jurisdictionName=(jurisdiction:ConversationJurisdiction)=>jurisdiction==='US-WY'?'Wyoming':jurisdiction==='US-DE'?'Delaware':jurisdiction==='EE'?'Estonia':'UK';

async function record(repo:Repository,actor:Actor,id:string){return owned((await repo.list<ConversationRecord>('agent_conversations',{id}))[0],actor);}
async function response(repo:Repository,conversation:ConversationRecord){const jurisdiction=supportedJurisdiction.parse(conversation.jurisdiction_code);const turns=await repo.list<ConversationTurn>('agent_conversation_turns',{conversation_id:conversation.id},{orderBy:'id',limit:200});return{conversation:{id:conversation.id,caseId:conversation.case_id,jurisdiction,status:conversation.status,executionMode:conversation.execution_mode,model:conversation.model,synthetic:conversation.synthetic,state:intakeSchemaFor(jurisdiction).parse(conversation.state_json),pendingPatch:z.record(z.string(),z.string()).parse(conversation.pending_patch),revision:conversation.revision,createdAt:conversation.created_at,updatedAt:conversation.updated_at},turns};}
function assertSandbox(sandbox:boolean){if(!sandbox)throw new DomainError('AGENT_SANDBOX_ONLY','La conversación agéntica está habilitada solo con datos ficticios en sandbox',403);}
function assertCustomer(actor:Actor){if(actor.role!=='customer')throw new DomainError('CUSTOMER_ACTION_REQUIRED','Solo el cliente puede iniciar, responder o confirmar su onboarding',403);}
async function checkedCase(repo:Repository,actor:Actor,id:string,revision?:number,expected?:ConversationJurisdiction){
 const item=owned((await repo.list<FormationRecord>('formation_cases',{id}))[0],actor);const jurisdiction=supportedJurisdiction.safeParse(item.jurisdiction_code);if(!jurisdiction.success)throw new DomainError('CASE_GUIDE_MISMATCH','El agente conversacional aún no está implementado para este expediente');if(expected&&jurisdiction.data!==expected)throw new DomainError('CASE_GUIDE_MISMATCH',`El expediente no corresponde a ${jurisdictionName(expected)}`);if(item.execution_mode!=='SANDBOX')throw new DomainError('AGENT_SANDBOX_ONLY','El expediente debe ser sintético',403);if(revision!==undefined&&item.revision!==revision)throw new DomainError('CONFLICT','El expediente cambió. Recarga antes de iniciar la conversación.',409);if(['CANCELLED','REJECTED'].includes(item.status))throw new DomainError('TERMINAL_CASE','El expediente no admite conversación',409);return{item,jurisdiction:jurisdiction.data};
}

async function start(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration,expected?:ConversationJurisdiction){
 assertSandbox(sandbox);assertCustomer(actor);const values=startSchema.parse(input);const prior=(await repo.list<ConversationRecord>('agent_conversations',{organization_id:actor.organizationId,client_request_id:values.clientRequestId}))[0];if(prior){if(prior.case_id!==values.caseId||(expected&&prior.jurisdiction_code!==expected))throw new DomainError('IDEMPOTENCY_CONFLICT','El identificador de solicitud ya pertenece a otro expediente',409);return response(repo,owned(prior,actor));}const{item:formation,jurisdiction}=await checkedCase(repo,actor,values.caseId,values.caseRevision,expected);const id=crypto.randomUUID();const mode=model?.key&&model.model?'OPENAI_RESPONSES':'DETERMINISTIC_MOCK';await repo.atomic([{kind:'insert',table:'agent_conversations',data:{id,organization_id:formation.organization_id,case_id:formation.id,jurisdiction_code:jurisdiction,status:'active',execution_mode:mode,model:mode==='OPENAI_RESPONSES'?model!.model:null,synthetic:true,state_json:initialConversationState(jurisdiction),pending_patch:{},revision:0,client_request_id:values.clientRequestId,created_by:actor.id}},{kind:'insert',table:'case_events',data:{organization_id:formation.organization_id,case_id:formation.id,event_type:'AGENT_CONVERSATION_STARTED',actor_type:actor.role,actor_user_id:actor.id,payload:{conversationId:id,jurisdiction,synthetic:true,mode,caseRevision:formation.revision}}}]);return response(repo,(await repo.list<ConversationRecord>('agent_conversations',{id}))[0]);
}
export async function startFormationConversation(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return start(repo,actor,sandbox,input,model);}
export async function startWyomingConversation(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return start(repo,actor,sandbox,input,model,'US-WY');}
export async function startDelawareConversation(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return start(repo,actor,sandbox,input,model,'US-DE');}
export async function startEstoniaConversation(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return start(repo,actor,sandbox,input,model,'EE');}
export async function startUkConversation(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return start(repo,actor,sandbox,input,model,'GB');}

async function get(repo:Repository,actor:Actor,sandbox:boolean,caseId:string,expected?:ConversationJurisdiction){assertSandbox(sandbox);const id=z.uuid().parse(caseId);await checkedCase(repo,actor,id,undefined,expected);const items=await repo.list<ConversationRecord>('agent_conversations',{case_id:id},{orderBy:'created_at',descending:true,limit:1});return items[0]?response(repo,owned(items[0],actor)):null;}
export async function getFormationConversation(repo:Repository,actor:Actor,sandbox:boolean,caseId:string){return get(repo,actor,sandbox,caseId);}
export async function getWyomingConversation(repo:Repository,actor:Actor,sandbox:boolean,caseId:string){return get(repo,actor,sandbox,caseId,'US-WY');}
export async function getDelawareConversation(repo:Repository,actor:Actor,sandbox:boolean,caseId:string){return get(repo,actor,sandbox,caseId,'US-DE');}
export async function getEstoniaConversation(repo:Repository,actor:Actor,sandbox:boolean,caseId:string){return get(repo,actor,sandbox,caseId,'EE');}
export async function getUkConversation(repo:Repository,actor:Actor,sandbox:boolean,caseId:string){return get(repo,actor,sandbox,caseId,'GB');}

async function send(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration,expected?:ConversationJurisdiction){
 assertSandbox(sandbox);assertCustomer(actor);const values=messageSchema.parse(input);const conversation=await record(repo,actor,values.conversationId);const jurisdiction=supportedJurisdiction.parse(conversation.jurisdiction_code);if(expected&&jurisdiction!==expected)throw new DomainError('CASE_GUIDE_MISMATCH','La conversación pertenece a otra jurisdicción');const duplicate=(await repo.list<ConversationTurn>('agent_conversation_turns',{conversation_id:conversation.id,client_request_id:values.clientRequestId}))[0];if(duplicate)return response(repo,conversation);if(conversation.revision!==values.revision)throw new DomainError('CONFLICT','La conversación cambió. Recarga antes de continuar.',409);if(conversation.status==='closed')throw new DomainError('CONVERSATION_CLOSED','La conversación está cerrada',409);if(Object.keys(z.record(z.string(),z.string()).parse(conversation.pending_patch)).length)throw new DomainError('CONFIRMATION_REQUIRED','Confirma o rechaza los datos propuestos antes de continuar.',409);const message=safeSyntheticMessage(values.message);let extraction=deterministicExtract(message,jurisdiction);if(model?.key&&model.model){try{extraction=await extractWithOpenAI(message,permittedFieldsForMessage(conversation.state_json,message,3,jurisdiction),model,model.fetcher??fetch,jurisdiction);}catch(error){if(!(error instanceof DomainError)||model.failureMode==='throw')throw error;extraction={updates:[],modelStatus:'EXTERNAL_BLOCKED'};}}const patch=Object.fromEntries(extraction.updates.map(update=>[update.field,update.value]));const assistant=assistantFor(conversation.state_json,extraction.updates,jurisdiction);await repo.atomic([{kind:'update',table:'agent_conversations',where:{id:conversation.id,revision:conversation.revision},data:{pending_patch:patch,revision:conversation.revision+1,status:'active'}},{kind:'insert',table:'agent_conversation_turns',data:{organization_id:conversation.organization_id,conversation_id:conversation.id,turn_kind:'USER_MESSAGE',customer_message:message,assistant_message:assistant,proposed_patch:patch,model_status:extraction.modelStatus,client_request_id:values.clientRequestId,created_by:actor.id}},{kind:'insert',table:'case_events',data:{organization_id:conversation.organization_id,case_id:conversation.case_id,event_type:'AGENT_TURN_COMPLETED',actor_type:actor.role,actor_user_id:actor.id,payload:{conversationId:conversation.id,jurisdiction,synthetic:true,modelStatus:extraction.modelStatus,proposedFields:extraction.updates.map(update=>update.field),conversationRevision:conversation.revision+1}}}]);return response(repo,(await repo.list<ConversationRecord>('agent_conversations',{id:conversation.id}))[0]);
}
export async function sendFormationMessage(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return send(repo,actor,sandbox,input,model);}
export async function sendWyomingMessage(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return send(repo,actor,sandbox,input,model,'US-WY');}
export async function sendDelawareMessage(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return send(repo,actor,sandbox,input,model,'US-DE');}
export async function sendEstoniaMessage(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return send(repo,actor,sandbox,input,model,'EE');}
export async function sendUkMessage(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,model?:OpenAIConfiguration){return send(repo,actor,sandbox,input,model,'GB');}

async function confirm(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,now=new Date(),expected?:ConversationJurisdiction){
 assertSandbox(sandbox);assertCustomer(actor);const values=confirmationSchema.parse(input);const conversation=await record(repo,actor,values.conversationId);const jurisdiction=supportedJurisdiction.parse(conversation.jurisdiction_code);if(expected&&jurisdiction!==expected)throw new DomainError('CASE_GUIDE_MISMATCH','La conversación pertenece a otra jurisdicción');const duplicate=(await repo.list<ConversationTurn>('agent_conversation_turns',{conversation_id:conversation.id,client_request_id:values.clientRequestId}))[0];if(duplicate)return response(repo,conversation);if(conversation.revision!==values.revision)throw new DomainError('CONFLICT','La conversación cambió. Recarga antes de confirmar.',409);const patch=z.record(z.string(),z.string()).parse(conversation.pending_patch);const allowed=new Set(fieldsForJurisdiction(jurisdiction).map(field=>field.key));if(Object.keys(patch).some(field=>!allowed.has(field as never)))throw new DomainError('INVALID_AGENT_PATCH','La propuesta contiene campos no permitidos',400);if(!Object.keys(patch).length)throw new DomainError('NOTHING_TO_CONFIRM','No hay datos propuestos para confirmar',409);const updates=Object.entries(patch).map(([field,value])=>({field:field as ProposedUpdate['field'],value,evidence:value}));const state=values.accept?applyConfirmedPatch(conversation.state_json,updates,jurisdiction):intakeSchemaFor(jurisdiction).parse(conversation.state_json);const missing=nextMissing(state,jurisdiction);const status=!missing?'ready_for_packet_review':'active';const assistant=values.accept?(missing?`Datos guardados. Siguiente: ${missing.label}.`:'Datos completos para preparar el paquete de revisión interno.'):'No guardé los datos propuestos. Puedes corregirlos y volver a intentarlo.';await repo.atomic([{kind:'update',table:'agent_conversations',where:{id:conversation.id,revision:conversation.revision},data:{state_json:state,pending_patch:{},revision:conversation.revision+1,status}},{kind:'insert',table:'agent_conversation_turns',data:{organization_id:conversation.organization_id,conversation_id:conversation.id,turn_kind:values.accept?'PATCH_ACCEPTED':'PATCH_REJECTED',customer_message:null,assistant_message:assistant,proposed_patch:patch,model_status:'NOT_APPLICABLE',client_request_id:values.clientRequestId,created_by:actor.id}},{kind:'insert',table:'case_events',data:{organization_id:conversation.organization_id,case_id:conversation.case_id,event_type:values.accept?'AGENT_PATCH_ACCEPTED':'AGENT_PATCH_REJECTED',actor_type:actor.role,actor_user_id:actor.id,payload:{conversationId:conversation.id,jurisdiction,synthetic:true,fields:Object.keys(patch),conversationRevision:conversation.revision+1}}}]);const current=(await repo.list<ConversationRecord>('agent_conversations',{id:conversation.id}))[0];const result=await response(repo,current);const binding={caseId:conversation.case_id,revision:Number((await repo.list<FormationRecord>('formation_cases',{id:conversation.case_id}))[0].revision)};const packetPreview=status!=='ready_for_packet_review'?null:jurisdiction==='US-DE'?prepareDelawarePacket(state,binding,now):jurisdiction==='EE'?prepareEstoniaPacket(state,binding,now):jurisdiction==='GB'?prepareUkPacket(state,binding,now):prepareWyomingPacket(state,binding,now);return{...result,packetPreview};
}
export async function confirmFormationPatch(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,now=new Date()){return confirm(repo,actor,sandbox,input,now);}
export async function confirmWyomingPatch(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,now=new Date()){return confirm(repo,actor,sandbox,input,now,'US-WY');}
export async function confirmDelawarePatch(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,now=new Date()){return confirm(repo,actor,sandbox,input,now,'US-DE');}
export async function confirmEstoniaPatch(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,now=new Date()){return confirm(repo,actor,sandbox,input,now,'EE');}
export async function confirmUkPatch(repo:Repository,actor:Actor,sandbox:boolean,input:unknown,now=new Date()){return confirm(repo,actor,sandbox,input,now,'GB');}
